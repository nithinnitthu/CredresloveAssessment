# Rename to .env in backend/ and fill values for production/testing
LLM_PROVIDER=mock            # "mock" or "openai"
OPENAI_API_KEY=              # set key when using OpenAI
OPENAI_MODEL=gpt-3.5-turbo
LLM_MAX_TOKENS=512
TTS_PROVIDER=gtts
